class CartItem {
  constructor(productTitle, quantity, productPrice, sum) {
    this.productTitle = productTitle;
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.sum = sum;
  }
}

export default CartItem;
